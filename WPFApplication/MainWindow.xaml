<Window x:Class="WPFApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFApplication"
        xmlns:converter="clr-namespace:WPFApplication.Converters"
        mc:Ignorable="d"
        Title="MainWindow" Height="668.4" Width="800">
    <Grid>
        <Border Padding="10">
            <StackPanel>
                <!-- Buttons - Open & Save -->
                <Button x:Name="Open" Grid.Column="0" Padding="0 5" Margin="20 10 20 0"  Content="Open" Command="{Binding ClickOpen}"/>
                <Button Padding="0 5" Margin="20 10 20 0"  Content="Settings" Command="{Binding SettingsOpen}"/>
                <!-- Labels -->
                

                <!-- Tree View -->
                <ContentControl Content="{Binding CurrentPage}"/>

            </StackPanel>
        </Border>
    </Grid>
</Window>
